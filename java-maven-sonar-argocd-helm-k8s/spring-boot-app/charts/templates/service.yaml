apiVersion: v1
kind: Service
metadata:
  name: {{ include "myssltest.fullname" . }}
  labels:
    {{- include "myssltest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myssltest.selectorLabels" . | nindent 4 }}
